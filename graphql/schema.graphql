type Query {
    movies(limit: Int!): [Movie]!
    movie(id: Int!): Movie!
}

type Movie {
    id: Int!
    title: String!
    year: Int!
    genres: [String]!
    rating: Float!
    poster: String!
    summary: String!
}